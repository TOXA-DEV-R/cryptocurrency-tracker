/** @format */

import React, { FC } from "react";
import { AuthModalCloseTypes } from "../../types/index";

const Login: FC<AuthModalCloseTypes> = ({ handleClose }) => {
  return <div>login</div>;
};

export default Login;
